<%= form_for(@player) do |f| %>        
        
  <%= f.label :name %>  
  <%= f.text_field :name %>

  <%= f.label :mark %>  
  <%= f.text_field :mark %>

  <%= f.submit "Create Player", class: "btn btn-large btn-primary" %>
<% end %>