<%= form_for(@tictactoe) do |f| %>
    <%= f.fields_for :players, @player do |p| %>
      <%= p.label :name %>
      <%= p.text_field :name %>      
    <% end %>
  <%= f.submit "Create Player", class: "btn btn-large btn-primary" %>
<% end %>        


